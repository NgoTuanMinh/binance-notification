Ok bây giờ chuyển các phần VolumeMonitor này vào 1 folder feature riêng và chuyển biến TELEGRAM_CHAT_ID thành TELEGRAM_NOTIFICATION_CHAT_ID, sau đó tiếp tục với feature khác:
- Tích hợp AI (gemini) vào để phân tích dữ liệu, tôi sẽ cung cấp API Key và TELEGRAM_BOT_CHAT_ID trong env.
-  Tôi đã tạo bot trong telegram với các command sau: 
+ start - Bắt đầu và hướng dẫn sử dụng
+ help - Xem hướng dẫn chi tiết
+ analyze - Phân tích coin (Cú pháp: /analyze SYMBOL TIMEFRAME)
- Lấy các thông tin cần thiết để gửi qua gemini phân tích
- Sử dụng 3 thư viện:
  1. ccxt: Để kết nối và lấy dữ liệu realtime từ các sàn giao dịch.
  2.technicalindicators hoặc tulind: Thư viện chuyên dụng để tính toán các chỉ báo kỹ thuật (EMA, RSI, MACD, v.v.) trong Node.js.
  3.@google/genai: Để giao tiếp với Gemini API.
- Hàm Thu Thập & Tính Toán Chỉ Báo
Hàm Node.js này sẽ lấy dữ liệu nến (OHLCV) từ sàn và tính toán tất cả các chỉ số bạn cần.
// crypto_analyzer.js

import { GoogleGenAI } from '@google/genai';
import ccxt from 'ccxt';
import * as ti from 'technicalindicators';

// ⚠️ THAY THẾ CÁC GIÁ TRỊ NÀY BẰNG KEY THỰC TẾ CỦA BẠN
const GEMINI_API_KEY = "YOUR_GEMINI_API_KEY"; 
const TELEGRAM_BOT_TOKEN = "YOUR_TELEGRAM_BOT_TOKEN"; // Cần cho phần Telegram Bot

// Cấu hình Gemini
const ai = new GoogleGenAI({ apiKey: GEMINI_API_KEY });
const model = 'gemini-pro';

// Khởi tạo sàn giao dịch (Ví dụ: Binance)
const binance = new ccxt.binance(); 

/**
 * Thu thập OHLCV và tính toán các chỉ báo kỹ thuật cần thiết.
 * @param {string} symbol - Ví dụ: 'ETH/USDT'
 * @param {string} timeframe - Ví dụ: '4h'
 * @returns {object|null} Dữ liệu kỹ thuật đã được tổng hợp.
 */
async function calculateIndicators(symbol, timeframe) {
    // 1. THU THẬP DỮ LIỆU THÔ (Cần đủ nến để tính toán, ví dụ 200 nến)
    let ohlcv;
    try {
        ohlcv = await binance.fetchOHLCV(symbol, timeframe, undefined, 200);
    } catch (e) {
        console.error(`Lỗi khi lấy dữ liệu OHLCV cho ${symbol}:`, e);
        return null;
    }

    if (!ohlcv || ohlcv.length < 100) {
        console.error('Không đủ dữ liệu nến để tính toán chỉ báo.');
        return null;
    }

    // Tách cột đóng cửa (close) và volume
    const close = ohlcv.map(candle => candle[4]);
    const volumes = ohlcv.map(candle => candle[5]);
    const highs = ohlcv.map(candle => candle[2]);
    const lows = ohlcv.map(candle => candle[3]);

    const currentPrice = close[close.length - 1];
    const lastVolume = volumes[volumes.length - 1];

    // 2. TÍNH TOÁN CÁC CHỈ BÁO KỸ THUẬT

    // --- EMA 34 và 89 ---
    const ema34 = ti.EMA.calculate({ period: 34, values: close });
    const ema89 = ti.EMA.calculate({ period: 89, values: close });
    const lastEma34 = ema34[ema34.length - 1];
    const lastEma89 = ema89[ema89.length - 1];

    // --- RSI(14) ---
    const rsi14 = ti.RSI.calculate({ period: 14, values: close });
    const lastRsi14 = rsi14[rsi14.length - 1];

    // --- MACD ---
    const macdResult = ti.MACD.calculate({
        fastPeriod: 12, slowPeriod: 26, signalPeriod: 9, values: close
    });
    const lastMacd = macdResult[macdResult.length - 1];
    const macdLine = lastMacd.MACD;
    const signalLine = lastMacd.signal;

    // --- Volume ---
    // Tính Volume trung bình của 20 nến trước đó (loại trừ nến hiện tại)
    const avgVolume = volumes.slice(-21, -1).reduce((sum, v) => sum + v, 0) / 20;

    // --- Hỗ trợ/Kháng cự (S/R) - Phương pháp đơn giản ---
    // Lấy đỉnh cao nhất (kháng cự) và đáy thấp nhất (hỗ trợ) của 50 nến gần nhất
    const recentHighs = highs.slice(-50, -1);
    const recentLows = lows.slice(-50, -1);
    const resistance = Math.max(...recentHighs);
    const support = Math.min(...recentLows);
    
    // 3. PHÂN TÍCH VÀ DIỄN GIẢI CHỈ BÁO (Tạo ra chuỗi văn bản cho Gemini)

    // --- Phân tích EMA ---
    let emaStatus = "";
    if (currentPrice > lastEma34 && currentPrice > lastEma89) {
        emaStatus = `Giá ($${currentPrice.toFixed(2)}) đang nằm trên cả EMA34 ($${lastEma34.toFixed(2)}) và EMA89 ($${lastEma89.toFixed(2)}), thể hiện xu hướng tăng.`;
        if (lastEma34 > lastEma89) {
            emaStatus += " (EMA34 đang cắt trên EMA89 - Gold Cross)";
        }
    } else if (currentPrice < lastEma34 && currentPrice < lastEma89) {
        emaStatus = `Giá ($${currentPrice.toFixed(2)}) đang nằm dưới cả EMA34 ($${lastEma34.toFixed(2)}) và EMA89 ($${lastEma89.toFixed(2)}), thể hiện xu hướng giảm.`;
    } else {
        emaStatus = "Giá đang dao động quanh các đường EMA, thị trường đang đi ngang.";
    }

    // --- Phân tích RSI ---
    let rsiSummary = "";
    if (lastRsi14 >= 70) {
        rsiSummary = `Đang ở mức ${lastRsi14.toFixed(2)}, vùng Quá mua (Overbought).`;
    } else if (lastRsi14 <= 30) {
        rsiSummary = `Đang ở mức ${lastRsi14.toFixed(2)}, vùng Quá bán (Oversold).`;
    } else {
        rsiSummary = `Đang ở mức ${lastRsi14.toFixed(2)}, vùng trung lập.`;
    }

    // --- Phân tích MACD ---
    let macdSummary = "";
    if (macdLine > signalLine && macdLine > 0) {
        macdSummary = `Đường MACD (${macdLine.toFixed(4)}) đang cắt lên trên đường Signal (${signalLine.toFixed(4)}) và nằm trên mức 0, xác nhận động lượng tăng.`;
    } else if (macdLine < signalLine && macdLine < 0) {
        macdSummary = `Đường MACD (${macdLine.toFixed(4)}) đang cắt xuống dưới đường Signal (${signalLine.toFixed(4)}) và nằm dưới mức 0, xác nhận động lượng giảm.`;
    } else {
        macdSummary = "Đang nằm gần mức 0, thị trường thiếu xu hướng rõ rệt.";
    }

    // --- Phân tích Volume ---
    const volumeDiffPercent = ((lastVolume - avgVolume) / avgVolume) * 100;
    let volumeSummary = `Volume hiện tại là **${lastVolume.toFixed(2)}** so với Volume trung bình (${avgVolume.toFixed(2)}). `;
    if (volumeDiffPercent >= 20) {
        volumeSummary += `Cao hơn **${volumeDiffPercent.toFixed(2)}%**, xác nhận sức mạnh của động thái giá hiện tại.`;
    } else if (volumeDiffPercent <= -20) {
        volumeSummary += `Thấp hơn **${Math.abs(volumeDiffPercent).toFixed(2)}%**, cho thấy sự thiếu quan tâm của thị trường.`;
    } else {
        volumeSummary += "Ở mức trung bình, chưa có sự xác nhận mạnh.";
    }

    // 4. TỔNG HỢP KẾT QUẢ CUỐI CÙNG
    return {
        symbol,
        timeframe,
        price: currentPrice,
        ema_summary: emaStatus,
        rsi_value: lastRsi14,
        rsi_summary: rsiSummary,
        macd_summary: macdSummary,
        volume_summary: volumeSummary,
        support: support,
        resistance: resistance,
    };
}
- Hàm Phân Tích Gemini (Sử dụng Dữ liệu đã tính)
Tiếp theo, bạn sử dụng kết quả từ hàm trên để xây dựng prompt và gửi đến Gemini API.
- Prompt mẫu:
Bạn hãy đóng vai một nhà phân tích kỹ thuật chuyên nghiệp. Tôi muốn phân tích về đồng Ethereum (ETH/USDT) trên khung thời gian H4 để tìm kiếm cơ hội giao dịch trong 24 giờ tới.
Dữ liệu kỹ thuật hiện tại:
Giá: $3,850.
EMA: Giá đang nằm trên EMA34 ($3,810) và EMA89 ($3,750). EMA34 vừa cắt lên EMA89.
RSI(14): Đang ở mức 68, hướng lên mạnh.
MACD: Đường MACD đã cắt lên đường Signal và nằm trên mức 0.
Volume: Cột volume của nến tăng gần nhất cao hơn 30% so với mức trung bình.
Hỗ trợ/Kháng cự: Vùng hỗ trợ gần nhất là $3,800. Vùng kháng cự mạnh là $4,000.
Dựa trên các thông tin trên, hãy:
Đánh giá xu hướng hiện tại của ETH (Tăng, Giảm, Đi ngang).
Đề xuất 2 phương án giao dịch (Mua/Long và Bán/Short) bao gồm:
Vùng vào lệnh (Entry).
Mục tiêu chốt lời (Take Profit).
Điểm dừng lỗ (Stop Loss).
Nêu rõ các rủi ro tiềm ẩn cho mỗi phương án.